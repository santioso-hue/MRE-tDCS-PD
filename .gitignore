# Nested git repos (already versioned separately)
tDCS-head-model/

# Large subject data directories
# These contain NIfTIs, meshes, and logs - too large for git, use DVC or paths doc
FullPD5_segmentation/
sub04_segmentation/
charm_ernie/

# Papers / literature
papers/

# Large binary/data file types
*.nii
*.nii.gz
*.msh
*.msh.opt
*.mat
*.npz
*.h5
*.hdf5

# SimNIBS outputs
*.geo
*.pos

# QC harness outputs (regenerable: re-run analysis/qc_harness.py)
analysis/qc/

# Sim figure outputs (regenerable: analysis/qc_figures.py)
analysis/sim_compare/

# Analysis result tables (regenerable: re-run the analysis scripts)
analysis/results/

# Patient data + result/figure artifacts (NEVER public; sensitive or regenerable)
data/
results/
figures/

# Local-only working docs (recreated per machine; never travel with a public clone)
CLAUDE.md
state.md
PROJECT_LAYOUT.md
docs/specs/
docs/plans/
docs/superpowers/

# FSL outputs
*.log

# macOS
.DS_Store
.AppleDouble
.LSOverride
__MACOSX/

# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
*.egg-info/
dist/
build/
venv/
.env

# Jupyter
.ipynb_checkpoints/

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Local machine config (paths, subject ID)
config/config.sh
# Cohort manifest carries scan-date subject IDs and ages - never tracked (config/cohort.example.json is the template)
config/cohort.json

# Pilot (FullPD5) - superseded by the cohort; kept locally for reference, not tracked
pipeline/run_qti_cov.m
references/
docs/results-deliverables.md

# Methods/theory writeup - the canonical version lives in the manuscript
pipeline/conductivity_models_derivation.md

# Internal bibliography
docs/REFERENCES.md

# Manuscript figure + table generators (the artifacts ship in the paper, not the method)
analysis/fig1_workflow.py
analysis/fig2_tensor_divergence.py
analysis/fig3_field_maps.py
analysis/fig4_cross_montage.py
analysis/fig5_mre_specificity.py
analysis/table1_montages.py
analysis/table2_to_latex.py
analysis/table_models.py
analysis/supp_tables.py

# Figure-only render helpers (imported only by the generators above)
analysis/_figstyle.py
analysis/_figsave.py
analysis/_repsubj.py
analysis/_montagerender.py
analysis/_surfrender.py

# Standalone diagnostics (regenerable; outputs already under the ignored analysis/results/)
analysis/qc_figures.py
analysis/mesh_qc.py
analysis/roi_audit.py
analysis/fa_sigma_diagnostic.py

# Unit tests not wired into the run_subject pipeline gates
tests/test_cohort_manifest.py
tests/test_mre_cohort_corr.py
